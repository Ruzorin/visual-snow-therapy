<Window x:Class="VisualSnowScreen.Overlay.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="VisualSnowOverlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        IsHitTestVisible="False"
        Focusable="False"
        Background="Transparent"
        RenderOptions.EdgeMode="Aliased"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <Grid>
        <!-- Gürültü Katmanı (WriteableBitmap) -->
        <Image x:Name="NoiseImage" Stretch="Fill" IsHitTestVisible="False" SnapsToDevicePixels="True" Visibility="Collapsed" Opacity="0.03"/>
        
        <!-- Renk Katmanı -->
        <Border x:Name="TintBorder" Background="#59E0A9AF" IsHitTestVisible="False"/>
    </Grid>
</Window>